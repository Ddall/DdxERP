{# OrehaERP - Allan - Oreha:ErpBundle:FamillePrestation:new.html.twig #}
{% extends "::orehaAdminTemplate.html.twig" %}
{% block title %}{{ parent()}} - Famille de Prestation{% endblock %}
{% block body%}
<div class="container-fluid">
    <div class="row-fluid">
        <div  class="span10">
            <fieldset>
                <legend>
                    Famille de Prestation
                    <a class="btn" href="{{ path('oreha_admin_famille_prestation') }}"><span class="icon-arrow-left"></span> Retour</a>
                </legend>
                <div class="well">
                    <form method="post" class="form form-horizontal" {{ form_enctype(formFamille) }}>
                        <div class="row-fluid">
                            {% for row in formFamille %}
                                {% if row.vars.name != '_token' %}
                                <div class="control-group">
                                    <div class="control-label">
                                        {{ form_label(row) }}
                                        {{ form_errors(row) }}
                                    </div>
                                    <div class="controls">
                                        {{ form_widget(row) }}
                                    </div>
                                </div>
                                {% else%}
                                    {{ form_widget(row)}}
                                {% endif %}
                            {% endfor%}
                        </div>
                        <input type="submit" class="btn btn-primary" />
                        <a class="btn" href="{{ path('oreha_admin_famille_prestation') }}">Annuler</a>
                    </form>
                </div>
            </fieldset>
        </div>
    </div>
</div>
{% endblock body %}