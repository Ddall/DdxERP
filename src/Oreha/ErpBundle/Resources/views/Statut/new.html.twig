{# OrehaERP - Allan - Oreha:ErpBundle:Statut:new.html.twig #}
{% extends "::orehaTemplate.html.twig" %}
{% block title %}{{ parent()}} - Mise à jour du statut{% endblock %}
{% block body%}
<div class="container-fluid">
    <div class="row-fluid">
        <div  class="span10">
            <fieldset><legend>Nouveau Statut</legend>
                <div class="well">
                    <form method="post" {{ form_enctype(formStatut) }}>
                        {{ form_widget(formStatut) }}
                        <input type="submit" class="btn btn-primary" />
                        <a class="btn" href="{{ path('oreha_erp_dossier', {'id': dossier.id}) }}">Annuler</a>
                    </form>
                </div>
            </fieldset>
        </div>
    </div>
</div>
{% endblock body %}