{# OrehaERP - Allan - Oreha:ErpBundle:CEE:modifier.html.twig #}
{% extends "::orehaTemplate.html.twig" %}
{% block title %}{{ parent()}} - Modifier un CEE{% endblock %}
{% block body%}
<div class="container-fluid">
    <div class="row-fluid">
        <div  class="span10">
            <fieldset>
                <legend>
                    Modifier l'aide CEE {{ cee.nom }} pour le devis du {{ cee.devis.date|date(format_date)}} ({{ cee.devis.dossier.intitule|upper}})
                    <a class="btn" href="{{ path('oreha_erp_dossier_devis_voir', {'id': devis.id}) }}"><span class="icon-arrow-left"></span> Retour</a>
                </legend>
                <div class="well">
                    <form method="post" class="form form-horizontal" {{ form_enctype(formCEE) }}>
                        <div class="row-fluid">
                            {% for row in formCEE %}
                                {% if row.vars.name != '_token' %}
                                <div class="control-group">
                                    <div class="control-label">
                                        {{ form_label(row) }}
                                        {{ form_errors(row) }}
                                    </div>
                                    <div class="controls">
                                        {{ form_widget(row) }}
                                    </div>
                                </div>
                                {% else%}
                                    {{ form_widget(row)}}
                                {% endif %}
                            {% endfor%}
                        </div>
                        <input type="submit" class="btn btn-primary" />
                        <a class="btn" href="{{ path('oreha_erp_dossier_devis_voir', {'id': devis.id}) }}">Annuler</a>
                    </form>
                </div>
            </fieldset>
        </div>
    </div>
</div>
{% endblock body %}